# snap-tools rebuild — do not auto-publish

This branch replaces the legacy static tool directory with the Astro MVP.
Do not merge to `main` until production publish is approved in virtual-office
(`approvals/` + `owner-actions/`).
